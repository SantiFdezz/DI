<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyOtherCatalog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <!-- CUSTOM THEME -->
    <style name="HomeTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/purple</item>
        <item name="colorAccent">@color/pink</item>
        <item name="backgroundColor"></item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="CellTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/red</item>
        <item name="colorAccent">@color/pink</item>
        <item name="android:text">@style/CustomText</item>
    </style>
    <style name="Theme.MyOtherCatalog" parent="Base.Theme.MyOtherCatalog" />
</resources>